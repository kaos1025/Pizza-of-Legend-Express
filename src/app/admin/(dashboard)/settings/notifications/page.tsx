import { NotificationSettings } from '@/components/admin/NotificationSettings';

export const metadata = {
  title: '알림 설정 - 피자오브레전드 관리자',
};

export default function NotificationSettingsPage() {
  return <NotificationSettings />;
}
